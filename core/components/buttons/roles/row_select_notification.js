module.exports = {
    data: {
        name: "row_select_notification"
    },
    async execute(interaction, interactionUser, guild) {
        switch (interaction.values[0]) {
            
        }
    }
}